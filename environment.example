# .env - Environment Variables for Hamco API
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git - it contains secrets!

# =============================================================================
# DATABASE (PostgreSQL)
# =============================================================================
# Railway will provide these, or use local PostgreSQL for development
DATABASE_PUBLIC_URL="postgresql://${{PGUSER}}:${{POSTGRES_PASSWORD}}@${{TCP_PROXY_DOMAIN}}:${{TCP_PROXY_PORT}}/${{PGDATABASE}}"
DATABASE_URL="postgresql://${{PGUSER}}:${{POSTGRES_PASSWORD}}@${{PRIVATE_DOMAIN}}:5432/${{PGDATABASE}}"
PGDATA="/var/lib/postgresql/data/pgdata"
PGDATABASE="${{POSTGRES_DB}}"
PGHOST="${{RAILWAY_PRIVATE_DOMAIN}}"
PGPASSWORD="${{POSTGRES_PASSWORD}}"
PGPORT="5432"
PGUSER="${{POSTGRES_USER}}"
POSTGRES_DB="whatevs"
POSTGRES_PASSWORD="iAmSUPerC00Liknow"
POSTGRES_USER="postgres"
RAILWAY_DEPLOYMENT_DRAINING_SECONDS="60"
SSL_CERT_DAYS="820"

# Full connection string (optional - will be built from above if not set)
# DATABASE_URL=postgres://user:password@host:port/database

# =============================================================================
# JWT AUTHENTICATION
# =============================================================================
# Generate a strong random key for production (at least 32 characters)
# Use: openssl rand -base64 32
JWT_KEY=your-super-secret-jwt-key-minimum-32-characters-long
JWT_ISSUER=hamco-api
JWT_AUDIENCE=hamco-client

# =============================================================================
# APPLICATION
# =============================================================================
# ASPNETCORE_ENVIRONMENT=Development
# ASPNETCORE_URLS=http://localhost:5000

# =============================================================================
# EMAIL (Future - Mailjet integration)
# =============================================================================
# MAILJET_API_KEY=your-mailjet-api-key
# MAILJET_SECRET_KEY=your-mailjet-secret-key
# FROM_EMAIL=noreply@hamco.app
# FROM_NAME=Hamco Blog
